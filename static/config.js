// Leave blank when the dashboard and API share a host during local development.
// For GitHub Pages, replace this value with the HTTPS URL of the hosted API.
globalThis.APP_CONFIG = Object.freeze({
  apiBaseUrl: "https://candlestick-alerts-app.onrender.com"
});

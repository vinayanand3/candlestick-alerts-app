rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Browsers never access Firestore directly. The Render service account uses
    // the server API, which is governed by IAM and bypasses these client rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
